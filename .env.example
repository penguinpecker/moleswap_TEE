# ══════════════════════════════════════════════════════════════
# MoleSwap v3 Configuration
# ══════════════════════════════════════════════════════════════

# ── Deployment ─────────────────────────────────────────────────
PRIVATE_KEY=0x...                     # Deployer/oracle wallet private key
ORACLE_ADDRESS=                       # Oracle address (defaults to deployer)
TEE_SIGNER=                           # TEE signer address (defaults to deployer for testnet)

# ── Arbitrum Sepolia ───────────────────────────────────────────
RPC_URL=https://sepolia-rollup.arbitrum.io/rpc
CHAIN_ID=421614
ARBISCAN_API_KEY=                     # For contract verification (optional)

# ── Deployed Contracts (fill after deployment) ─────────────────
HOOK_ADDRESS=0x...                    # MoleSwapHook address
TOKEN0=0x...                          # Token0 (lower address)
TOKEN1=0x...                          # Token1 (higher address)

# ── Oracle Settings ────────────────────────────────────────────
BATCH_INTERVAL_MS=30000               # How often to batch intents (30s)
RELEASE_CHECK_INTERVAL_MS=10000       # How often to check releases (10s)
USE_LOCAL_TEE=true                    # Use local TEE simulation for testnet
HTTP_PORT=3001                        # Oracle HTTP API port

# ── Pool Settings ──────────────────────────────────────────────
POOL_MANAGER=0xFB3e0C6F74eB1a21CC1Da29aeC80D2Dfe6C9a317  # V4 PoolManager on Arb Sepolia
